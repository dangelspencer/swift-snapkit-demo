# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swift-snapkit-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swift-snapkit-demo
  pod 'SnapKit'

  target 'swift-snapkit-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swift-snapkit-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
